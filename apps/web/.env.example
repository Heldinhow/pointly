# Opcional: sobrescreve o endpoint do WebSocket.
# Padrão: em dev usa ws://<host>:3001/ws; em prod usa a mesma origem (/ws).
VITE_WS_URL=

# Opcional: base da API REST.
# Padrão: /api/v1 (o proxy do Vite encaminha para :3001 em dev).
VITE_API_BASE=
