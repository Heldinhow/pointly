# Google Analytics 4 — Measurement ID
# Vazio = analytics desabilitado (no-op puro, zero request a googletagmanager.com)
# Em prod, setado via Dokploy project env → build-arg → inlined no bundle pelo Vite.
# Formato: G-XXXXXXXXXX (1-10 chars alfanuméricos após o prefixo)
VITE_GA_MEASUREMENT_ID=